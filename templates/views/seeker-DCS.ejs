<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Complete Services</title>
    <link rel="stylesheet" href="/css/seeker-DJ.css">
    <!--Google Font-->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Ubuntu:wght@300&display=swap" rel="stylesheet"> 
    <!--Bootstrap-->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <style>
        #loader-wrapper {
              position: fixed;
              top: 0;
              left: 0;
              width: 100%;
              height: 100%;
              background-color: rgba(0, 0, 0, 0.8); /* Light black background */
              display: flex;
              justify-content: center;
              align-items: center;
              z-index: 1000;
            }
        
            .loader {
              display: flex;
              justify-content: space-around;
              width: 60px;
            }
        
            .dot {
              width: 15px;
              height: 15px;
              background-color: white;
              border-radius: 50%;
              animation: blink 1.4s infinite both;
            }
        
            .dot:nth-child(1) {
              animation-delay: 0s;
            }
        
            .dot:nth-child(2) {
              animation-delay: 0.2s;
            }
        
            .dot:nth-child(3) {
              animation-delay: 0.4s;
            }
        
            @keyframes blink {
              0%, 80%, 100% {
                opacity: 0;
              }
              40% {
                opacity: 1;
              }
            }
            .rating-form {
    direction: rtl;
    unicode-bidi: bidi-override;
    font-size: 2rem;
    margin: 20px 0;
}

.rating-form input[type="radio"] {
    display: none;
}

.rating-form label {
    color: #ccc;
    cursor: pointer;
}

.rating-form label:hover,
.rating-form label:hover ~ label,
.rating-form input[type="radio"]:checked ~ label {
    color: #f5b301;
}

.rating-value {
    margin-top: 1rem;
    font-size: 1.5rem;
}

/* Modal styles */
.modal {
    display: none;
    position: fixed;
    z-index: 1;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    overflow: auto;
    background-color: rgba(0,0,0,0.4);
    animation: fadeIn 0.3s;
}

@keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}

.modal-content {
    background-color: #fff;
    margin: 10% auto;
    padding: 20px;
    border: 1px solid #888;
    width: 80%;
    max-width: 500px;
    text-align: center;
    border-radius: 10px;
    box-shadow: 0 5px 15px rgba(0,0,0,0.3);
    animation: slideIn 0.3s;
}

@keyframes slideIn {
    from { transform: translateY(-50px); opacity: 0; }
    to { transform: translateY(0); opacity: 1; }
}

.close-btn {
    color: #aaa;
    float: right;
    font-size: 28px;
    font-weight: bold;
}

.close-btn:hover,
.close-btn:focus {
    color: black;
    text-decoration: none;
    cursor: pointer;

}
        </style>
        
          </head>
        <body>
        <div id="loader-wrapper">
            <div class="loader">
              <div class="dot"></div>
              <div class="dot"></div>
              <div class="dot"></div>
            </div>
          </div><%- include('seeker-navigation.ejs') %>

<% if(job.length == 0) { %>
<h3 class="unknown"><b>You have no Complete Service</b></h3>
<% } else { %>
    <h3 class="known"><b>Services Completed (<%= job.length %>)</b></h3>
    

      <!--New End-->
    <% for (var x = 0; x < job.length; x++) { %>
        
	
        <div class="container">

            <div class="apply-box">
                <div style="display: inline-block; background-color: red; border: 2px solid red; border-radius: 5px; padding: 5px;">
                    <p style="color: white; margin: 0;"><%= job[x]["status"] %></p>
                </div>
                <% if (job[x]["status"] == "Complete") { %>
                  <div style="margin-left: 73.5%; display: inline-block; background-color: blue; border: 2px solid blue; border-radius: 5px; padding: 5px;">
                      <p style="color: white; margin: 0;"><b>Payment: <%= job[x]["payment"] %></b></p>
                  </div>
                  <% } %>
                      <div class="form-container">
	      <div class="formm-control">
                            <h4><b>Service Needed: </b> <%= job[x]["skill"] %></h4>
                        </div>
	       <div class="formm-control">
                            
                        </div>
                        <div class="formm-control">
                            <p><b>Name : </b> <%= job[x]["fullname"] %></p>
                        </div>
                        <div class="formm-control">
                            <p><b>Email :</b> <%= job[x]["email"] %></p>
                        </div>
                        <div class="formm-control">
                            <p><b>Gender :</b> <%= job[x]["gender"] %></p>
                        </div>
                        <div class="formm-control">
                            <p><b>Posted On: </b> <%= job[x]["date"].toISOString().split('T')[0] %></p>
                        </div>
	        <div class="formm-control">
                            <p><b>Start Date :</b> <%= job[x]["start_date"].toISOString().split('T')[0] %></p>
                        </div>
	        <div class="formm-control">
                            <p><b>End Date :</b> <%= job[x]["end_date"].toISOString().split('T')[0] %></p>
                        </div>
	        <div class="formm-control">
                            <p><b>Start Time :</b> <%= job[x]["start_time"] %></p>
                        </div>
	        <div class="formm-control">
                            <p><b>End Time :</b> <%= job[x]["end_time"] %></p>
                        </div>
	        <div class="formm-control">
                            <p><b>Language Preference :</b> <%= job[x]["language"] %></p>
                        </div>
                        <div class="formm-control">
                            <p><b>Price (Rs): </b><%= job[x]["price"] %></p>
                        </div>
                        
	        <div class="formm-control">
                            <p><b>Address :</b> <%= job[x]["address"] %></p>
                        </div>
	        <div class="formm-control">
                            <p><b>Description about Service:</b> <%= job[x]["detail"] %></p>
                        </div>
	        
                        <% if (job[x]["status"] == "Unassigned") { %>
                            <div class="formm-control">
                                <a href="/seeker/seekerDJR?reply=<%= job[x]["reply"] %>&skill=<%= job[x]["skill"] %>&id=<%= job[x]["_id"] %>&status=Unassigned" class="btn btn-primary border-0 fw-bold mt-2 px-4 login-btn rounded-2 " style="margin-right: 250px;">Request: <%= job[x]["reply"].length %></a>
                                        </div>
                        <% } else { %>
                        <div class="formm-control">
                            <a href="/seeker/seekerDJR?assignProv=<%= job[x]["assignProv"] %>&skill=<%= job[x]["skill"] %>&id=<%= job[x]["_id"] %>&status=In Progress" class="btn btn-primary border-0 fw-bold mt-2 px-4 login-btn rounded-2 " style="margin-right: 250px;">See Profile</a>
                          </div>
                          <% if (job[x]["rating"] == 0 ||  job[x]["rating"] == undefined) { %>
                          <div class="formm-control">
                            <button class="btn btn-info border-0 fw-bold mt-2 px-4 login-btn rounded-2 " style="margin-left: 250px;" id="open-modal-btn">Rate Provider</button>
        <div id="rating-modal" class="modal">
            <div class="modal-content">
                <span class="close-btn">&times;</span>
                <form class="rating-form">
                    <input type="radio" name="rating" id="star5" value="5"><label for="star5" title="5 stars">★</label>
                    <input type="radio" name="rating" id="star4" value="4"><label for="star4" title="4 stars">★</label>
                    <input type="radio" name="rating" id="star3" value="3"><label for="star3" title="3 stars">★</label>
                    <input type="radio" name="rating" id="star2" value="2"><label for="star2" title="2 stars">★</label>
                    <input type="radio" name="rating" id="star1" value="1"><label for="star1" title="1 star">★</label>
                    <input type="hidden" id="prov" value="<%= job[x]["assignProv"] %>">
                    <input type="hidden" id="job" value="<%= job[x]["_id"] %>">
                    <button class="btn btn-info border-0 fw-bold mt-2 px-4 login-btn rounded-2 " style="margin-left: 10px;" type="button" id="submit-rating-btn">Submit Rating</button>
                </form>
            </div>
        </div>
        <p class="rating-value" id="rating-value"></p>
                          </div>
                            <% } if (job[x]["payment"] == "Pending") { %>
                              <div class="formm-control">
                            <form action="/seeker/payment?price=<%= job[x]["price"] %>*100&id=<%= job[x]["_id"] %>" method="POST">
                              <script
                                src="//checkout.stripe.com/v2/checkout.js"
                                class="stripe-button"
                                data-key="<%= key %>"
                                data-amount="<%= job[x]['price'] %>*100"
                                data-currency="pkr"
                                data-name="<%= job[x]['skill'] %>"
                                data-description="Congrats on completing Service"
                                data-locale="auto" >
                                </script>
                              </form>
                                    </div>
                        <% } }%>
                        <% if (job[x]["status"] == "Unassigned" || job[x]["status"] == "Unapproved  ") { %>
                        <div class="formm-control">
                            <a href="/seeker/seekerDJD?job=<%= job[x]["_id"] %>" class="btn btn-danger border-0 fw-bold mt-2 px-4 login-btn rounded-2 " style="margin-left: 250px;">Delete</a>
                                    </div>
                                    <% } %>
                    </div>
                    
                    </div>
               
            </div>
        </div>
<%}}%>


<!--Footer-->

<%- include('seeker-footer.ejs') %>     

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
        <script>
            document.addEventListener("DOMContentLoaded", function() {
              setTimeout(function() {
                document.getElementById('loader-wrapper').style.display = 'none';
                document.getElementById('content').style.display = 'block';
              }, 1000); // Adjust the timeout duration as needed
            });
          </script>
          <script>
            // script.js
document.addEventListener('DOMContentLoaded', () => {
    const openModalBtn = document.getElementById('open-modal-btn');
    const modal = document.getElementById('rating-modal');
    const closeModalBtn = document.querySelector('.close-btn');
    const submitRatingBtn = document.getElementById('submit-rating-btn');
    const ratingValueDisplay = document.getElementById('rating-value');

    openModalBtn.addEventListener('click', () => {
        modal.style.display = 'block';
    });

    closeModalBtn.addEventListener('click', () => {
        modal.style.display = 'none';
    });

    window.addEventListener('click', (event) => {
        if (event.target === modal) {
            modal.style.display = 'none';
        }
    });

    submitRatingBtn.addEventListener('click', async () => {
        const selectedRating = document.querySelector('.rating-form input[name="rating"]:checked');
        if (selectedRating) {
          console.log('Selected Rating:', selectedRating.value);
            ratingValueDisplay.textContent = selectedRating.value;
            modal.style.display = 'none';
            const prov = document.getElementById('prov').value;
            const job = document.getElementById('job').value;
            fetch(`http://localhost:3302/seeker/rating?value=${selectedRating.value}&prov=${prov}&job=${job}`) // must update according to port number
    .then((response) => response.json())
    .then(window.location.href = 'http://localhost:3302/seeker/dashboard')
            console.log(selectedRating.value);
        } else {
            alert('Please select a rating');
        }
    });
});

          </script>
    </body>
</html>

